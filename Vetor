#include <stdio.h>
int main ()
{
int vetor[10];
int i=0, maior=0;


for (i=0; i<=9; i++) {

      printf("\n Entre com o numero:");
      scanf("%d", &vetor[i]);

      if (vetor[i] > maior)
        maior= vetor[i];

    }
        printf("O maior numero eh %d", maior);




return 0;






}
